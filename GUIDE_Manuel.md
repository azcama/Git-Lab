# Documentation Report - Manuel
## Commands used to create the branch
I used the command: `git checkout -b feature/Manuel`

## Staging and Committing process
1. I used `git add GUIDE_Manuel.md` to stage the file.
2. I used `git commit -m "Add documentation guide"` to save the changes.

## Merge Conflicts
To resolve a conflict, I would pull the latest changes from 'develop', manually edit the file to fix the differences, and then add and commit the resolved version.